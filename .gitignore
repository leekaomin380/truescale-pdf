# 投递/渲染临时产物（正常落 /tmp，此处兜底）
quaderno_delivery_*.pdf
quaderno_render_*.log
.DS_Store

# 根目录下的临时预览图不入库，但 docs/assets 下的文档插图必须保留
/*.png

# GUI 打包产物（由 gui/build-app.sh 重建）
gui/*.app/

# 可重建的 GitHub Release 上传资产
/dist/
